# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MakeFairytale' do
  # Comment the next line if you don't want to use dynamic frameworks
  #use_frameworks!
  user_modular_headers!	
  # Pods for MakeFairytale

pod 'Firebase/Database'
  pod 'Firebase/Auth',:modular_headers => true
  pod 'Firebase/Core',:modular_headers => true
  pod 'Firebase/Analytics',:modular_headers => true
  pod 'Firebase/Storage',:modular_headers => true
  pod 'FirebaseUI/Storage',:modular_headers => true
  pod 'Firebase/Firestore',:modular_headers => true
  pod 'Firebase/Messaging',:modular_headers => true
  pod 'SDWebImage', :modular_headers => true
  pod 'Kingfisher', '~> 5.9',:modular_headers => true
  pod 'SnapKit', '~> 5.0.0',:modular_headers => true
  pod 'ObjectMapper', '~> 3.5',:modular_headers => true

end